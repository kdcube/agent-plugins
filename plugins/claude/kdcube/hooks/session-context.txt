kdcube active. Before any bundle work in this session: (1) read tier1/00-pack-contract.md once for the agent guardrails, then tier1/01-navigate.md for routing into deeper kdcube docs; (2) for any KDCube product/SDK/architecture/operator knowledge, use the kdcube-docs skill: read the LOCAL kdcube-ai-app repo (docs + source) resolved via config/repos.yaml — there is no knowledge service; if the repo is not resolvable, run /kdcube:init (the default repo is the CLI-managed runtime checkout at <workdir>/repo via kdcube info; it pins that or bootstraps a runtime; a hand-set repos.yaml path is the developer override — do not silently pin a checkout merely found on disk); (3) honor the working-environment preflight from tier1/02-test.md before any python3 / pytest invocation; (4) use /kdcube:runtime-init for kdcube init + secrets bootstrap, /kdcube:bundle-configure for kdcube bundle source/config/secrets, and /kdcube:bundle-maintain for existing bundle work; (5) never store real secrets in descriptors/docs/logs and do not use deprecated enabled_config or the phrase bundle iframe as a bundle type; (6) widget/generated HTML API clients must use KDCube runtime baseUrl or their own frame origin, never window.top/document.referrer/host-page origin; (7) bundle-local Python imports must be package-relative (from .pkg import x, from ..pkg import y) — never a bare top-level import (from pkg import x) and never a try/except ImportError fallback to a top-level import (a bundle is always loaded as a package, so there is no flat-load case; the shared bundle suite '-k import_contract' rejects it at release). (8) before writing any widget/UI: settle the design first — use the kdcube design system (tokens/look from solutions/chat/ui/widget or @kdcube/components-react), build modular React+Redux (store/slices/typed hooks/api layer/feature components, never a monolithic App.tsx with inline fetch), support every host it runs in (kdcube iframe operations route AND, if applicable, the Telegram Mini App public route + initData + ready/expand + safe areas), and copy the SDK `memories` widget as the template; see 11-common-failures.md 'Widget UI — Settle The Design Before You Build'; (9) economics is a NORMAL authoring concern, not a rare subsystem: if the app has a chat/agent turn, semantic search, task execution, or a paid background job, derive from BaseEntrypointWithEconomics / BaseEntrypointWithEconomicsAndMemory, pass model_service=self.search_model_service(flow='<surface>.search') into searchable components (which call embed_search_query(...), with lexical fallback when it returns None), wrap paid background work in EconomicsGuard(...), never hand-roll per-component guards, and keep lexical task search (SQLite FTS/BM25) free of embedding economics; verify [economics.enforcement] traces (plan_resolved / admit / reserve_ok / settle / deny) — see 12-economics.md. The bundle-builder skill at skills/bundle-builder/SKILL.md is the unified planning role; do not split into sub-personas.
