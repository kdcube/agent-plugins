{
  "description": "SessionStart hook that injects the kdcube-dev MCP usage contract, so docs-first behavior fires even when the kdcube workflow skill's trigger description doesn't match the user's phrasing.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks-handlers/session-start.sh\""
          }
        ]
      }
    ]
  }
}
